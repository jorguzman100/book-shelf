<section class="auth-layout">
  <article class="memphis-panel auth-panel">
    <p class="section-tag">Welcome back</p>
    <h2>Log In</h2>
    <p>Continue where you left off and pick up your next read.</p>

    <form class="login">
      <div class="form-group">
        <label for="email-input">Email address</label>
        <input
          type="email"
          class="form-control"
          id="email-input"
          placeholder="name@email.com"
          autocomplete="email"
        />
      </div>
      <div class="form-group">
        <label for="password-input">Password</label>
        <input
          type="password"
          class="form-control"
          id="password-input"
          placeholder="Your password"
          autocomplete="current-password"
        />
      </div>
      <div
        style="display: none;"
        id="alert"
        class="alert alert-danger"
        role="alert"
      >
        <span class="msg"></span>
      </div>
      <button type="submit" class="btn btn-default">Log in</button>
    </form>

    <p class="mt-3 mb-0">Need an account? <a href="/">Sign up here</a>.</p>
  </article>

  <aside class="memphis-panel auth-panel auth-visual">
    <h2>What You Get</h2>
    <ul class="auth-features-list">
      <li>One-tap category browsing and quick add-to-cart flow.</li>
      <li>Persistent shopping cart synced to your account.</li>
      <li>Purchase history to track every book you buy.</li>
    </ul>
    <p class="auth-badge">Memphis Shelf Reader</p>
  </aside>
</section>
